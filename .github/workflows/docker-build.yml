name: Debug Docker Build
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build backend docker image
        run: docker build -t apex-intel-backend -f backend/Dockerfile backend/
